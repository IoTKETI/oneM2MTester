control
{
  //local definitions
  var <TypeId> vl_<variableName> := c_<constName>;

  if (execute (tc_<MTestCaseName1>(vl_<variableName>)) != fail)
  {
    execute(tc_<MTestCaseName2>(vl_<variableName>))
  }
  else
  {
    execute(tc_<MTestCaseName3>())
  }
}  // control
